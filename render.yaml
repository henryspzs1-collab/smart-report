services:
  - type: web
    name: smart-report
    runtime: python
    plan: starter
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn smart_report:app
    envVars:
      - key: DATA_FILE
        value: /data/bateria_data.json
    disk:
      name: smart-report-data
      mountPath: /data
      sizeGB: 1
